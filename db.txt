You are acting as a Senior AI Engineer implementing a production-oriented RAG pipeline.

TASK: Implement ONLY Qdrant Storage Sub-phase 3 — Parent/Child Point Storage.

ARCHITECTURE IS LOCKED:

Loading
→ Chunking
→ Embedding
→ Qdrant Storage
→ Retrieval
→ Generation

COMPLETED:
- Q1: Qdrant connection
- Q2: Qdrant collections
- Collections:
    parent_chunks
    child_chunks
- Both collections:
    vector size = 384
    distance = COSINE
- Embedding output:
    EmbeddedChunkGroup
- Parent and Child embeddings are already generated.
- Existing Document metadata must be preserved.

DO NOT IMPLEMENT:
- similarity search
- top-k retrieval
- query embedding
- metadata filtering/search
- ACTIVE/INACTIVE switching
- generation lifecycle
- re-ingestion workflow
- deletion
- Groq/LLM
- evaluation

Those belong to later phases.

==================================================
1. INPUT
==================================================

Use the existing:

    list[EmbeddedChunkGroup]

Each group contains:

    parent:
        document: Document
        embedding: list[float]

    children:
        list of:
            document: Document
            embedding: list[float]

==================================================
2. PARENT STORAGE
==================================================

Store every Parent in:

    parent_chunks

Each Qdrant point must contain:

    point_id
    vector
    payload

Vector:
    use the already-generated Parent embedding.

Payload:
    preserve the Parent Document metadata without deleting,
    renaming, fabricating, or unnecessarily modifying fields.

At minimum preserve existing fields such as:

    document_id
    document_checksum
    source_file
    source_path
    domain
    document_type
    loader_used
    page_number
    character_count
    is_empty_page
    ingested_at

    parent_id
    parent_index
    parent_checksum
    chunking_config_hash

    page_start
    page_end
    status
    version
    updated_at
    indexed_at

Only include fields that actually exist in the Document metadata.
Do not fabricate missing values.

Store the Parent page content in the Parent payload because the later
retrieval flow needs the Parent context.

==================================================
3. CHILD STORAGE
==================================================

Store every Child in:

    child_chunks

Each Qdrant point must contain:

    point_id
    vector
    payload

Vector:
    use the already-generated Child embedding.

Payload:
    preserve the Child Document metadata.

At minimum preserve existing fields such as:

    document_id
    document_checksum
    source_file
    source_path
    domain
    document_type
    loader_used
    page_number
    character_count
    is_empty_page
    ingested_at

    parent_id
    parent_index
    parent_checksum

    child_id
    child_index
    child_checksum

    chunking_config_hash
    page_start
    page_end
    status
    version
    updated_at
    indexed_at

Only include fields that actually exist.
Do not fabricate metadata.

IMPORTANT:
Do NOT store parent_content in every Child payload.

The Parent content is stored once in parent_chunks.

The Child must retain:

    parent_id

so Retrieval can later perform:

    Child result
        ↓
    parent_id
        ↓
    Parent lookup
        ↓
    Parent context

==================================================
4. DETERMINISTIC POINT IDs — LOCKED
==================================================

DO NOT use uuid4() for Qdrant Point IDs.

DO NOT use parent_id or child_id as the deterministic Point ID.

Use UUID5 with one stable project namespace.

PARENT POINT ID:

    UUID5(
        namespace,
        "parent:"
        + document_id
        + ":"
        + document_checksum
        + ":"
        + chunking_config_hash
        + ":"
        + parent_index
    )

CHILD POINT ID:

    UUID5(
        namespace,
        "child:"
        + document_id
        + ":"
        + document_checksum
        + ":"
        + chunking_config_hash
        + ":"
        + parent_index
        + ":"
        + child_index
    )

Use explicit string conversion for indexes where required.

Keep the exact "parent:" and "child:" prefixes so Parent and Child
identities cannot collide.

The namespace must be a stable constant/configuration value and must
NOT be randomly generated for each execution.

Do not include:
    parent_id
    child_id
    embedding values
    page content

in the deterministic Point ID.

==================================================
5. UPSERT
==================================================

Use Qdrant upsert operations.

The operation must be safe to run repeatedly with the same input.

Because Point IDs are deterministic:

    same document
    + same document checksum
    + same chunking_config_hash
    + same parent/child index
        ↓
    same Point ID
        ↓
    upsert updates the existing logical point
    rather than creating a duplicate Point ID.

Do not implement the complete re-ingestion/generation workflow yet.
That is the next Storage phase.

==================================================
6. PUBLIC STORAGE FUNCTION
==================================================

Create a clean public function that the future orchestrator can call,
for example:

    store_embedded_chunks(...)

Follow the project's existing naming, typing, module, and logger conventions.

Do not redesign EmbeddedChunkGroup.

==================================================
7. VALIDATION
==================================================

Before upserting, validate:

- vector is a list of floats;
- vector dimension is 384;
- required identity fields needed for the Point ID exist;
- Parent/Child metadata relationship is preserved.

If a required identity field is missing, fail clearly rather than
fabricating an ID.

Do not log:
- page content;
- parent content;
- child content;
- embedding vectors;
- Qdrant API key.

==================================================
8. TESTS
==================================================

Add focused unit tests proving:

1. Parent points go to parent_chunks.
2. Child points go to child_chunks.
3. Parent vector is stored unchanged.
4. Child vectors are stored unchanged.
5. Parent metadata is preserved.
6. Child metadata is preserved.
7. Child parent_id is preserved.
8. Parent and Child content are stored in their appropriate payloads.
9. Parent content is NOT duplicated into Child payloads.
10. Same input produces the same Parent Point ID.
11. Same input produces the same Child Point ID.
12. Changing document_checksum changes the Point ID.
13. Changing chunking_config_hash changes the Point ID.
14. Changing parent_index changes the Parent Point ID.
15. Changing child_index changes the Child Point ID.
16. Parent and Child Point IDs cannot collide because of their prefixes.
17. Missing required identity metadata fails clearly.
18. No API key, vector, or page content is written to logs.

Use mocks/fakes for Qdrant in unit tests where appropriate.

Do not require live Qdrant credentials for unit tests.

==================================================
9. IMPORTANT ARCHITECTURAL CONSTRAINT
==================================================

Our Parent/Child architecture is LOCKED:

    Qdrant
    ├── parent_chunks
    │      └── Parent vector + Parent payload/content
    │
    └── child_chunks
           └── Child vector + Child payload
                         │
                         └── parent_id
                               ↓
                         Parent lookup later

Do NOT introduce SQLite or another database.

Do NOT put Parent content into every Child.

Do NOT combine Parent and Child into one collection.

Do NOT move similarity search into Storage.

==================================================
10. RUN AND REPORT
==================================================

Run:

    uv run python -m unittest tests.unit.test_qdrant_storage

Also run the existing relevant tests:

    uv run python -m unittest tests.unit.test_qdrant_collections

    uv run python -m unittest tests.unit.test_embeddings

Report:

- exact files changed;
- storage function created;
- Point-ID implementation;
- tests run;
- exact test results;
- any limitation.

STOP after Q3.

Do not implement Q4 generation/re-ingestion/ACTIVE-INACTIVE yet.
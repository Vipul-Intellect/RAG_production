You are acting as a Senior AI Engineer implementing a production-oriented RAG ingestion pipeline.

TASK: Implement ONLY Qdrant Storage Sub-phase 1 — Qdrant Connection.

PROJECT ARCHITECTURE IS LOCKED:
Loading → Chunking → Embedding → Qdrant Storage → Retrieval → Generation

Do NOT implement collections, upsert, Point IDs, Parent/Child storage, re-ingestion,
generations, ACTIVE/INACTIVE lifecycle, retrieval, or evaluation in this sub-phase.

CURRENT STATE:
- Loading phase is implemented.
- Chunking phase is implemented.
- Parent/Child chunk hierarchy is implemented.
- Embedding phase is implemented and tested.
- Embedding model: BAAI/bge-small-en-v1.5
- Embedding dimension: 384.
- Qdrant Cloud Free cluster has already been created.
- Qdrant URL and API key are available locally.
- Credentials must NEVER be hard-coded or committed.

IMPLEMENT ONLY:

1. Add the Qdrant dependency using the project's existing package/dependency
   management approach.

2. Add environment-based configuration:
   QDRANT_URL
   QDRANT_API_KEY

3. Create a small, reusable Qdrant client/connection module.

4. Load credentials from environment variables.
   - Fail clearly if required variables are missing.
   - Never expose the API key in logs or exceptions.
   - Do not log the full URL if it could contain credentials.

5. Provide a clean public function/class that the future Storage orchestrator
   can use to obtain the Qdrant client.

6. Add a lightweight connection/health verification function so we can verify
   that the configured Qdrant service is reachable.

7. Follow the project's existing architecture, naming conventions, logger,
   error-handling style, and Python version.

8. Do not create collections yet.
   Do not write vectors yet.
   Do not create payload indexes yet.

TESTS:
Add focused unit tests for:
- missing QDRANT_URL
- missing QDRANT_API_KEY
- valid configuration
- API key is never included in logs/errors
- client creation uses the configured URL
- connection verification handles success/failure cleanly

If a real Qdrant integration test requires live credentials, keep it separate
from unit tests and do not hard-code credentials.

IMPORTANT CONSTRAINTS:
- Make minimal changes.
- Do not refactor unrelated Loading, Chunking, or Embedding code.
- Do not redesign the architecture.
- Do not invent additional databases.
- Do not use SQLite for this phase.
- Do not create a local Qdrant fallback; this project is using the configured
  Qdrant Cloud service.
- Keep secrets out of Git.
- Update .gitignore if necessary to ensure .env is ignored.

Run the relevant tests using the project's existing test command, and report:
1. Files changed.
2. Dependency added.
3. Environment variables required.
4. Tests run and exact results.
5. Any remaining limitation.

STOP after Q1. Do not proceed to Q2.